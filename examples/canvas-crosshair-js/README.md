# Canvas Crosshair (JavaScript) Example

The JavaScript counterpart to the FuncScript crosshair scene. It reads the `canvas` value hook to match the view to the canvas size and draws horizontal/vertical center lines.

## Setup

```bash
cd examples/canvas-crosshair-js
npm install
```

## Run

```bash
npm run play
```
