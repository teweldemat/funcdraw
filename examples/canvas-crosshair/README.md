# Canvas Crosshair Example

This scene reads FuncDraw Play's `canvas` value hook to size its view to the actual canvas pixel dimensions, then draws horizontal and vertical lines through the center.

## Setup

```bash
cd examples/canvas-crosshair
npm install
```

## Run

```bash
npm run play
```

Resize the browser window to see the crosshair adjust in real time.
