# Rectangle Drawing (JavaScript) Example

This variation renders the rectangle scene from a JavaScript expression to ensure the FuncScript JavaScript binding works inside FuncDraw.

## Setup

```bash
cd examples/rectangle-drawing-js
npm install
```

## Run

```bash
npm run play
```

`funcdraw-play` evaluates `art/scene.js`, which exports a JavaScript snippet returning the scene's view and graphics data.
