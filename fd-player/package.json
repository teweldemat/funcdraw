{
  "name": "@funcdraw/fd-player",
  "version": "0.1.0",
  "description": "Runtime player that renders FuncDraw models onto an HTML canvas",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "prepare": "npm run build",
    "test": "vitest run"
  },
  "dependencies": {
    "@funcdraw/core": "^0.1.0",
    "@tewelde/funcscript": "^0.2.5",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
